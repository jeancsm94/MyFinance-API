import { ICollection} from "./collections-list";
import { Document } from 'mongodb'

export const lancamentoCollection: ICollection = { name: 'lancamentos'}
export const tipoLancamentoCollection: ICollection = { name: 'tipoLancamentos'}